{
  "name": "react-app-rewired",
  "version": "1.5.2",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "publish": "lerna publish && lerna-npm-readme-hack"
  },
  "devDependencies": {
    "lerna": "2.0.0"
  }
}
